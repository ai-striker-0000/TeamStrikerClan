/*var url = "/errors/warning.html";
    function rtclickcheck(keyp) {
      if (navigator.appName == "Netscape" && keyp.which == 3) { alert("NOPE!!'-'"),window.location = url;; return false; }
      if (navigator.appVersion.indexOf("MSIE") != -1 && event.button == 2) { alert("NOPE!!'-'"),window.location = url;; return false; }
    }
    document.onmousedown = rtclickcheck;*/

    